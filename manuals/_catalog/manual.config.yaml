# Block gallery.
#
# Hand-written fixtures rendered through the real pipeline, so what you see is
# what the system actually produces.
#
# The list being hand-written is the known flaw: a block type added to
# `packages/blocks` does not appear here on its own. Update this file whenever
# the catalogue changes. Closing that gap is what `packages/catalog` is for.

version: 1

manual:
  id: _catalog
  source: broadlineavida
  title: Catálogo de estructuras
  product: Broadsec SIMM
  # THE FALLBACK ONLY. A manual that carries a change log prints the highest row
  # of that log instead of this field (`deliveredVersion`, cli/src/main.ts);
  # this manual has none, so this is what its cover and filename use.
  #
  # THE OWNER AUTHORISES EVERY CHANGE TO IT. A version marks a DELIVERY to the
  # client, never work done internally. See `manuals/AGENTS.md`, "Versioning".
  contentVersion: 0.1.0
  language: es
  register: neutral-formal

catalog:
  version: null

axes:
  tenant:
    label: Deployment
    values:
      - { id: demo, code: DEMO, name: Demostración }

targets:
  - { tenant: demo }

output:
  dir: output
  filename: "catalogo-estructuras-v{contentVersion}.pdf"
