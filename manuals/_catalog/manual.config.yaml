# Block gallery — PROVISIONAL.
#
# Hand-written fixtures rendered through the real pipeline, so what you see is
# what the system actually produces. packages/catalog will eventually generate
# this FROM the catalogue; until then a hand-listed gallery can go stale, which
# is exactly why it is marked provisional.

version: 1

manual:
  id: _catalog
  source: broadlineavida
  title: Catálogo de estructuras
  product: Broadsec SIMM
  contentVersion: 0.1.0
  language: es
  register: neutral-formal

catalog:
  version: null

axes:
  tenant:
    label: Deployment
    values:
      - { id: demo, code: DEMO, name: Demostración }

targets:
  - { tenant: demo }

output:
  dir: output
  filename: "catalogo-estructuras-v{contentVersion}.pdf"
